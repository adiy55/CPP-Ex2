#ifndef CPP_EX2_CONSTANTS_HPP
#define CPP_EX2_CONSTANTS_HPP

// global constants for vital values in the assignment
namespace constants {
    const int ROW_LENGTH = 100;
    const char TILDA = '~';
    const char UNDERSCORE = '_';
}
/*
 * When including a header, the preprocessor pastes its contents into the file.
 * I used these constants to simplify my code implementation, so it makes more sense to include them
 * only where they are needed.
 */

#endif //CPP_EX2_CONSTANTS_HPP
